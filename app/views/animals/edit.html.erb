<div class="container">

  <div class="row">
    <div class="col-12">
      <%= simple_form_for @animal, url: animal_path, method: :patch do |f| %>
        <h2>Edit Animal</h2>
        <%= f.input :name, label: "Name:" %>
        <%= f.input :species, label: "Species:" %>
        <%= f.input :dangerosity, label: "Dangerosity:" %>
        <%= f.input :needs, label: "Needs:" %>
        <%= f.input :photo, as: :file %>
          <%= f.button :submit, value: 'Save changes', class:"btn btn-primary", "data-disable-with" => "Creating.."%>
      <% end %>
    </div>
  </div>

</div>
